name: Update Profile README

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests beautifulsoup4

      - name: Run updater script
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
          GITHUB_USERNAME: inetanel
        run: python .github/scripts/update_profile.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md generated-langs.svg generated-activity.svg generated-trophies.svg block-articles.svg block-articles-header.svg block-articles-footer.svg block-projects-header.svg block-projects-footer.svg block-contact.svg block-article-*.svg block-project-*.svg
          git diff --staged --quiet || (
            git commit -m "chore: auto-update profile [$(date +'%Y-%m-%d')]" &&
            git pull --rebase origin main &&
            git push
          )
